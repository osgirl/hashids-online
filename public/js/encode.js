// Generated by CoffeeScript 1.6.3
(function() {
  var encode, j;

  j = jQuery.noConflict();

  encode = function() {
    var hashids, i, id, min_len, n, numbers, nums, salt;
    salt = j('#salt').val();
    min_len = parseInt(j('#min-length').val());
    hashids = new Hashids(salt, min_len);
    nums = j('#numbers').val().split(',');
    numbers = (function() {
      var _results;
      _results = [];
      for (i in nums) {
        n = nums[i];
        _results.push(parseInt(n));
      }
      return _results;
    })();
    id = hashids.encode.apply(hashids, numbers);
    return j('#result').val(id);
  };

  j(document).ready(function() {
    return j('#calc').on('click', function(event) {
      encode();
      return event.preventDefault();
    });
  });

}).call(this);
